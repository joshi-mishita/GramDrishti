# frontend/

React + TypeScript + Vite app (frontend person). Created in session S3.

Will hold: the officer console (map, priority list, review, verification, impact) and the mobile farmer app (English, Hindi, Punjabi, offline, printable bulletin). Response types are generated from `../contract/openapi.json`.
