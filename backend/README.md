# backend/

Python package `gramdrishti` (backend person). Created in session S1.

Will hold: data loaders and QC, baselines, features, downscaling models, uncertainty and reconciliation, agro-variables, the YAML advisory engine, SQLite review store, verification job and the FastAPI app served at `/api/v1`.

Model artifacts and snapshots go in `backend/artifacts/` and are git-ignored.
