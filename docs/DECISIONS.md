# Decisions

Judgment calls made during the build. Add a row whenever a choice is not dictated by the guides or `CLAUDE.md`.

| ID | Date | Session | Decision | Why | Alternatives considered | Revisit when |
|---|---|---|---|---|---|---|
| D001 | 2026-09-24 | S0 | Licence: MIT, copyright "GramDrishti contributors". | Chosen by the team. | Apache-2.0, GPL-3.0, none. | n/a |
| D002 | 2026-09-24 | S0 | Mock data files sit directly in `data/`; the source zip and `data/synthetic_oracle/` are git-ignored. | `CLAUDE.md` and the session prompts expect `data/README.md` and `data/generate_mock_data.py`; the oracle is the answer key (4.4 MB) and can be regenerated. | Commit the oracle; keep the zip's nested folder. | If CI ever needs the oracle, generate it in the job. |
| D003 | 2026-09-24 | S0 | CI uses a `detect` job that sets outputs; backend and frontend jobs are skipped when their project files do not exist. | Job-level `if` cannot inspect files. Skipped jobs count as passing for required checks, so branch protection works from day one. | `hashFiles` in step-level `if` on every step. | n/a |
| D004 | 2026-09-24 | S1 | B2 station anomalies are static per-station offsets per season group, fitted on TRAIN (station minus block reference, lapse-detrended for temperature; log ratio for rain and wind). IDW power 2 over all stations, 1 km distance floor, stations inside the target Panchayat excluded. | On the valid date the station observation does not exist yet, so same-day anomalies would leak. A trailing-window anomaly against the forecast mostly adds block-level forecast noise. Static offsets carry the persistent local signal. | Same-day anomalies (leak); trailing 7 to 14 day anomalies vs lead-1 forecast; nearest-k stations. | S5, if the model needs recent-state features. |
| D005 | 2026-09-24 | S1 | Bias-correction and station-offset target: mock mode uses the synthetic block truth (proxy target, TRAIN only); real mode uses the block mean of QC-cleaned station observations. | Rule 5 allows the oracle as a proxy training target in mock mode. Block truth does not exist for real data. | Station block means in both modes. | When real stations arrive. |
| D006 | 2026-09-24 | S1 | Season groups for additive bias: winter (Dec-Feb), pre-monsoon (Mar-May), monsoon (Jun-Sep), post-monsoon (Oct-Nov). Rain quantile mapping: monsoon (Jun-Sep) and the rest. Wind ratio per source and lead as ratio of sums, which makes the TRAIN mean bias zero. Inverse-MSE weights per variable and lead, from corrected TRAIN forecasts. | Guide 5.1 names "season group" without defining it; four groups keep at least about 360 pairs per group in TRAIN. | Two groups (hot/cool); monthly. | If CALIB bias per season stays large. |
| D007 | 2026-09-24 | S1 | Rain quantile mapping: wet day >= 1.0 mm. If the forecast is dry more often than observed, `cut` becomes the smallest positive forecast. If a group has no wet observations, the corrected forecast is 0. NaN stays NaN. | The guide's formula gives `cut = 0` in that case, which would map dry forecasts to wet amounts. | Drop the group. | n/a |
| D008 | 2026-09-24 | S1 | Real data files live in `data/real/` (git-ignored) with the same column names; names listed in `FILES["real"]` in `gramdrishti/data/config.py`. `GRAMDRISHTI_DATA_DIR` overrides the data folder. | Keeps real data out of git and makes the switch one env var. | Overwrite the mock files. | When real data arrives. |
| D009 | 2026-09-24 | S1 | `select_window(df, "TEST", ...)` raises `PermissionError` unless `allow_test=True`. Only the verification session passes it. | Makes rule 5 enforceable in code, not only by convention. | Convention only. | n/a |
| D010 | 2026-09-24 | S1 | Generator default `OUT` is its own folder (`data/`). CI regenerates the oracle into a temp folder and copies only `synthetic_oracle/`, so committed files are not touched. | Fresh clones and CI need the oracle for tests; the old default pointed at a path that does not exist locally. | Commit the oracle. | n/a |
| D011 | 2026-09-24 | S1 | Dependencies declared in `backend/pyproject.toml` with minimum versions, not pinned. Local dev ran Python 3.13; CI runs 3.11. | Pinning before the stack settles causes churn; the guide says to freeze once everything works. | `pip freeze` now. | S14 (Docker): pin with a lock file. |
| D012 | 2026-09-24 | S1 | QC: a consistency failure marks both tmax and tmin bad; stuck-sensor ignores zero values; spike compares with the previous calendar day only (no spike across a gap); monthly missing counts use calendar days in the data's overall range, so absent rows count as missing. `clean_values` sets failed values to NaN, rows are kept. | Guide 3.1 gives rules, not these edge cases. | Flag only tmin on inconsistency. | When real station data shows other failure modes. |
| D013 | 2026-09-25 | S2 | `session-02-contract-api` branches from `session-01-backend-data-baselines`, not `main`. | S1's PR is not merged yet and S2 serves B1 from S1's code. Merge S1 first; S2's PR then shows only S2 commits. | Wait for the S1 merge; copy code. | When S1 merges (rebase is not needed, the history stays linear). |
| D014 | 2026-09-25 | S2 | PROVISIONAL forecast until S5/S6: `p50` = B1 (corrected block forecast, fitted on TRAIN); `p10`/`p90` = p50 -/+ max(spread of the two bias-corrected sources, floor per variable), rain upper side x1.5, clipped to physical bounds; event probabilities from a piecewise-linear CDF through (p10, 0.1), (p50, 0.5), (p90, 0.9). Code: `gramdrishti/provisional/forecast.py`. Every response says `provenance: "provisional"`. | The session prompt asks for B1 values with a simple two-source spread. The band is uncalibrated, so it is labelled, not presented as a result. B1 is block-level, so all Panchayats in a block share values and rule 6 holds exactly. | B0 values; B2 (varies within a block but breaks block consistency before reconciliation exists). | S6 replaces it with snapshots. |
| D015 | 2026-09-25 | S2 | Quantile `block` and map `block_value` are the issued block forecast (plain mean of the NWP sources, B0); `delta = p50 - block_value`. | The product is compared against the plain block forecast, so "block" should be what a block-level service would issue today. With B1 as p50 this also makes the difference view non-trivial. | `block` = B1 (delta always 0 until S5). | If the team prefers the corrected block as the reference. |
| D016 | 2026-09-25 | S2 | Demo issue dates are picked by `python -m gramdrishti.contract.pick_demo_dates` from the issued block forecast (B0) only. 6 dates in TEST, 2 labelled training-period replays (January cold night, March wheat heat). Forecast endpoints accept only these dates (404 `issue_date_not_available` otherwise). | Scoring on issued forecasts reads no TEST outcomes, so picking TEST dates does not open TEST for evaluation. A fixed list keeps the stub and the later snapshots (S6) aligned. A test checks the picker never calls the oracle or observation loaders. | Pick from truth (would look at TEST outcomes); allow any date. | S6 if snapshot dates change. |
| D017 | 2026-09-25 | S2 | `data_mode` is a required field on every JSON response (a foreign member on GeoJSON) and a response header `X-Data-Mode`. A `provenance` field (`provisional`, `placeholder`, `computed`) marks where numbers come from. Placeholder endpoints (verification, impact, explain) answer 503 `not_computed` when `DATA_MODE=real`. | Rule 1 needs `data_mode` everywhere. `provenance` lets the UI flag generated numbers, and fake numbers can never appear under `data_mode: "real"` (rule 2). | `data_mode` on /meta only; no provenance. | n/a |
| D018 | 2026-09-25 | S2 | Shapes for endpoints Appendix A lists without a payload (/health, /geo properties, /observed, /risk, /advisories list, /farmers, /farmers/advice, /feedback response, /forecast/changes, /verification/coverage, /verification/regions, /data-quality) are defined in `schemas.py` and listed in `contract/CHANGELOG.md` v0.1.1. Additive: `/impact?decision=` and `/meta.issue_date_info`. POST endpoints answer 200. | The frontend needs every shape frozen to build. Everything is additive to Appendix A; no Appendix A field was renamed or removed. | Leave them undefined until each session. | Frontend review of the contract PR. |
| D019 | 2026-09-25 | S2 | PLACEHOLDER risk scores (`provisional/risk.py`) with placeholder thresholds: heavy rain P(>=35 mm); waterlogging P(>=10 mm) x drainage weight; heat from Tmax p90 35-45 C; frost from Tmin p10 4 to -2 C; dry spell from 5-day p50 rain < 10 mm, capped at "high" in monsoon and "moderate" otherwise. Levels cut at 0.25/0.5/0.75. Placeholder advisories come from a small template set for Panchayats whose top risk within 3 days is high or severe, one per crop in season. Review state and feedback live in memory. | The screens need realistic lists now; S8 replaces this with YAML rules and SQLite. All payloads carry `thresholds_status: "placeholder"`. | Empty lists until S8. | S8. |
| D020 | 2026-09-25 | S2 | `/observed` returns the Panchayat's own station (QC-cleaned; rain gauges give null for other variables), otherwise synthetic truth in mock mode (`source` says which), for any date including the TEST window. | The guide asks for the observed overlay. It is display only: no metric, model or threshold in this session reads it. | Hide TEST-window observations until S10. | S10, if the team wants the overlay hidden before verification. |
| D021 | 2026-09-25 | S2 | `/explain` placeholder: rank the Panchayat's static features by z-score against its block, keep those with a physical prior for the variable, top 3, `method: "placeholder_static_contrast"`. | Gives the panel real per-Panchayat content without pretending to be SHAP. | Random reasons; empty list. | S10 (SHAP). |
| D022 | 2026-09-25 | S2 | Derived agro-variables in the provisional forecast: THI = NRC 1971 formula on daily mean temperature (p50 Tmax and Tmin) and mean RH; ET0 = Hargreaves-Samani with Ra from latitude and day of year. `soil_moisture_frac` and `waterlog_risk` are null until S6. | Both are closed-form from the forecast. A water balance belongs to S6. | Null for all. | S6. |
| D023 | 2026-09-25 | S2 | Examples are generated by calling the real app (`make_examples`) with a fixed clock; numbers rounded to 2 decimals, GeoJSON coordinates to 5 decimals and written compact. Tests fail when `openapi.json` or any example is stale, when an example does not validate, or when an endpoint lacks an example. | Keeps examples identical to real responses and reproducible. | Hand-written examples. | n/a |
| D024 | 2026-09-25 | S2 | Hindi and Punjabi strings in `provisional/texts.py` are drafts by Claude Code; advisories carry `translation_status: "needs_native_review"`. | Rule 3. | English only. | S3/S13 native review. |
| D036 | 2026-09-26 | S5 | S5 works in a separate git worktree (`../GramDrishti-s05`) on `session-05-model-core`, branched from `origin/main` (S1 and S2 merged). Decision numbers start at D036 because the unmerged S3 branch already uses D025-D035. | The main checkout held uncommitted S4 frontend work; switching branches there could have disturbed it. | Stash S4 work; branch in place. | n/a |
| D037 | 2026-09-26 | S5 | Features beyond Guide section 4: block-relative static contrasts (`<feature>_rel` = Panchayat minus block mean) for elevation, TPI, irrigation, urban, water, tree and sand; NDVI and LST relative to the block; data age of the satellite value. Station rain sums (`stn_rain_prev3_mm`, `stn_rain_prev7_mm`) use days D-7..D-1 only (the issue day is excluded because its total is not complete at issue time). A satellite week counts as available on `week_start + 7 days`. | The model predicts departures from the block forecast, so contrasts with the rest of the block carry the signal directly. Excluding day D is the conservative reading of "available at issue time". | Raw static values only; include day D. | If real-time observations for day D become available at issue time. |
| D038 | 2026-09-26 | S5 | Purge (Guide 6.2): a TRAIN or CALIB table keeps an issue date only when all five valid dates fall inside the window. TRAIN issue dates 2023-01-01..2024-04-25, CALIB 2024-05-01..2024-07-10. | No valid date and no target crosses a window boundary. | Split by valid date only (lead 3-5 rows of late-April issues would sit in CALIB with a TRAIN issue date). | n/a |
| D039 | 2026-09-26 | S5 | The B1 block Tmin/Tmax used as the reconciliation target is repaired to a diurnal range of at least 1.0 C (the QC minimum) around its midpoint. B0 stays as issued. | The issued mock sources forecast Tmin >= Tmax on some rainy days (B0 316 rows, B1 214 rows in the TRAIN table). Reconciling to such a target forces Panchayats to break Tmin < Tmax (47 CALIB rows before the fix, 0 after). | Leave B1 as is and break Tmin < Tmax; clip only Panchayat values (breaks block consistency). | When real NWP arrives, check how often it happens. |
| D040 | 2026-09-26 | S5 | Dew point is reconciled additively (as in the guide). RH is then derived from the reconciled mean temperature and dew point and reconciled again with a bounded additive step (water-filling within 0..100 %) to the B1 block RH, so block consistency holds for all five served variables. RH quantiles come from dew point quantiles at the mean temperature. | Rule 6 asks for block consistency of Panchayat values; deriving RH alone does not preserve the block mean of RH. | Reconcile only dew point. | n/a |
| D041 | 2026-09-26 | S5 | Block consistency (< 1e-6) is guaranteed for the reconciled `mean`, as in Guide 7.2. The same shift or ratio is applied to p10/p50/p90, but the block mean of p50 is not exactly the block forecast. | The guide reconciles with the mean-model output. | Reconcile p50 separately (quantiles could cross the mean). | S6: decide whether the API's point value is `mean` (consistent) or `p50`. |
| D042 | 2026-09-26 | S5 | CQR offsets per variable and lead group (days 1-2, 3-5) only, no season split. Development coverage is measured by splitting CALIB by valid date into halves (2024-05-01..06-07 and 06-08..07-15), fitting offsets on one half and scoring the other, in both directions. Production offsets use all of CALIB. | CALIB covers May to mid-July only: pre-monsoon and monsoon onset, never winter. Splitting by valid date keeps the truth days disjoint. | Random halves (leak between neighbouring days); season groups. | S10 reports coverage on TEST once. |
| D043 | 2026-09-26 | S5 | Leave-one-block-out on TRAIN refits the mean models only (6 folds). The B1 bias correction stays fitted on all TRAIN blocks, so B0, B1 and the model share the same block forecast and differ only in how it is spread over Panchayats. B1 on TRAIN is therefore in-sample for bias. | Tests generalisation to unseen locations, which is the downscaling question; reconciliation needs the block forecast anyway. | Refit bias per fold. | If bias correction itself needs a spatial check. |
| D044 | 2026-09-26 | S5 | Multiplicative reconciliation: when the model's block mean is ~0 but the block forecast is not, the difference is added evenly (every Panchayat gets the block value). | The ratio is undefined there, and the guide's fallback (ratio 1) breaks block consistency. | Ratio 1. | n/a |
| D045 | 2026-09-26 | S5 | Reproducibility: LightGBM with `random_state=42`, `deterministic=True`, `force_row_wise=True`. The data hash is sha256 over the exact TRAIN feature/target rows and CALIB feature/outcome rows; model version `s5-lgbm-<first 10 hex of hash>`. Artifacts (`backend/artifacts/`, git-ignored): `model_<target>.joblib`, `events.joblib`, `bias.joblib`, `conformal.json`, `features.json`, `config.json`, `dev_report.txt`. | Same inputs, same parameters and the same machine give the same model (a test checks it). Hashing file bytes would include TEST-window rows of the oracle. | Hash the input files. | S14: pin library versions. |
| D025 | 2026-09-25 | S3 | `session-03-frontend-foundation` branches from `session-02-contract-api`, not `main`. | `main` has neither S1 nor S2; the frontend builds on S2's `contract/openapi.json` and `contract/examples/`. Merge S1, S2, then S3. | Wait for merges; copy the contract into the branch. | When S2 merges. |
| D026 | 2026-09-25 | S3 | Frontend toolchain: Node 22 LTS (`.nvmrc`, CI reads it), TypeScript pinned to 5.9, Vitest 4.1. | Node 20 reached end of life in April 2026 (the guide says 20). TypeScript 7 is not supported by typescript-eslint (<6.1) or openapi-typescript (^5). Vitest 5 is held back by the jest-dom peer range. | Node 20; TypeScript 7 without type-aware lint. | S14 when pinning for Docker. |
| D027 | 2026-09-25 | S3 | `frontend/public/mock/` is git-ignored and filled by `npm run sync:mock` before `dev` and `build`. `src/api/schema.d.ts` is committed; CI fails if it differs from a fresh `gen:types`. | `contract/examples/` stays the single source; committed types let the build run without the generator, the CI check catches drift. | Commit a copy of the examples; generate types at build time only. | n/a |
| D028 | 2026-09-25 | S3 | Mock and snapshot requests are resolved through the folder's `index.json` by path plus sorted query. No match gives `ApiError` code `not_in_mock`, shown as an empty state that lists the issue dates that do have files. Error examples replay as errors. Snapshot mode reads `/snapshot/index.json` in the same format. | A plain file-per-endpoint switch would show 9 Sep 2024 data under any chosen date. The snapshot format is an assumption for the backend export (S12). | Guide 9.1 `mockFile` argument per hook. | S12: backend confirms the snapshot `index.json` format. |
| D029 | 2026-09-25 | S3 | The app opens on issue date 2024-09-09 (`VITE_DEFAULT_ISSUE_DATE`) when the URL has none, or the last available date if the API does not offer it. A URL date the API does not offer is replaced. | Main demo date of contract v0.1.1 and the one most examples cover; `/meta` has no "default" field. | Latest date (few examples). | If `/meta` gains a default date. |
| D030 | 2026-09-25 | S3 | Dates use fixed day and month tables for en, hi and pa (not Intl): "Tue 10 Sep 2024", "मंगल 10 सितंबर 2024", "ਮੰਗਲ 10 ਸਤੰਬਰ 2024". Western digits in all languages. Parsed as UTC. | Chrome's Intl has no Punjabi month names (screenshots showed "2024 M09 9 Mon") while Node's does, so unit tests could not catch it. Full month names match the API's advisory text. | Intl.DateTimeFormat with hi-IN/pa-IN. | Native review of the names. |
| D031 | 2026-09-25 | S3 | Additional design tokens beyond Guide 2.3 (brand shades, ribbon tint, hover, skeleton, type scale, layout sizes), listed in `docs/design.md` and contrast-tested. | The guide palette has no hover, selected or ribbon colours; deriving them from guide colours keeps the look consistent. | Ad-hoc colours in components (forbidden by rule 10). | n/a |
| D032 | 2026-09-25 | S3 | URL keys `date`, `var`, `pid`. Role is derived from the route (`/farmer*` = farmer); the role switch navigates and keeps the query. The client sends `X-Role` to the real API. | Guide 5 asks for issueDate, variable and selectedPid in the URL; tying role to the route keeps links reproducible. | Role in the URL query. | n/a |
| D033 | 2026-09-25 | S3 | Placeholder screens show only counts, method, period, rules and notes from the API. Verification and impact numbers with `provenance: placeholder` are never displayed; provisional forecast and risk counts are shown with a provenance notice. | Rule 2: no invented accuracy on screen. | Show the placeholder tables marked as fake. | S11 when the verification job exists. |
| D034 | 2026-09-25 | S3 | `npm run shots` uses Playwright's Chromium by default and the installed Google Chrome with `SHOTS_BROWSER_CHANNEL=chrome`. Screenshots are full-page. | Here the Chromium download fails with a self-signed certificate in the chain (TLS interception); disabling certificate checks was not acceptable. | `NODE_TLS_REJECT_UNAUTHORIZED=0`. | n/a |
| D035 | 2026-09-25 | S3 | `maplibre-gl`, `recharts` and `vite-plugin-pwa` are installed but not used yet. | Session asks for the CLAUDE.md package set; the map (S4), charts (S7) and PWA (S13) use them. | Install when first used. | n/a |
| D036 | 2026-09-26 | S4 | `session-04-map-explorer` branches from `session-03-frontend-foundation`, not `main`. | `main` has none of S1-S3. Merge S1, S2, S3, then S4. | Wait for merges. | When S3 merges. |
| D037 | 2026-09-26 | S4 | Map fill colours interpolate linearly between the ramp's break values (Guide 8.1), and the legend draws the same ramp as an SVG colour bar with ticks at the breaks. Palettes are ColorBrewer with the darkest class dropped (RdBu drops both extremes). All map colours live in `src/lib/ramps.ts`; map chrome colours come from the design tokens at runtime. | One config drives both map and legend, so they cannot disagree. A legend colour bar encodes data; the "no gradients" rule targets decoration. The darkest classes merged with the dark block outlines. | Stepped classes (would hide small block-to-Panchayat changes). | If the team prefers classed colours. |
| D038 | 2026-09-26 | S4 | Tmax and Tmin breaks are every 3 C over the values on screen (block and Panchayat values of the chosen day together, at least two steps), not a fixed seasonal table. Block and Panchayat views share one ramp. | Guide 8.2 says "each 3 C over the seasonal range" without the range; data-driven 3 C steps fit every demo date. A shared ramp keeps the toggle honest: only polygons change, never the legend. | Fixed per-season breaks. | When real data sets seasonal ranges. |
| D039 | 2026-09-26 | S4 | Difference ramp: symmetric around 0, half-width = largest absolute difference rounded up to 1, 2, 2.5 or 5 x 10^k, at least 1 mm rain, 0.5 C temperature, 2 % humidity, 1 km/h wind; five stops (-m, -m/2, 0, m/2, m). Blue means wetter (rain, humidity); red means warmer or windier. | Round ticks read correctly (seven stops printed ±1.67 as "2"). The floor stops a 0.1 C difference from looking dramatic. | Seven stops; one direction convention for all variables. | n/a |
| D040 | 2026-09-26 | S4 | URL also carries `day` (lead day) and `view` (block, delta), both left out at their defaults (day 1, Panchayat view). | Links and screenshots reproduce a view mode; defaults stay out so other screens' links are unchanged. | Keep them in the store only. | n/a |
| D041 | 2026-09-26 | S4 | MapLibre 6 worker: excluded from Vite pre-bundling in dev (it then finds its own worker file); in production `setWorkerUrl` gets the worker bundled with `?worker&url`. | MapLibre 6 resolves its worker next to its module. Pre-bundling moved the module, and the dev worker transform adds an absolute import that cannot resolve inside MapLibre's blob worker. | CDN worker URL (external dependency). | On a MapLibre upgrade. |
| D042 | 2026-09-26 | S4 | Right panel headline, block value and difference come from the `/forecast/map` row already on screen; the all-variables table comes from `/forecast/panchayat/{id}`. | The headline works for every Panchayat at once and costs no request. In mock mode only MP0103, MP0302, MP0305 and MP0412 have a Panchayat forecast file; the others show the "no demo file" state. | Five `/forecast/map` calls for the table. | S7 (fan chart uses the same endpoint). |
| D043 | 2026-09-26 | S4 | The map shows a note whenever every Panchayat in each block has the same p50 (detected from the data), saying the Panchayat view varies only between blocks until the downscaling model. | The provisional forecast is block-level (D014), so the block and Panchayat views look almost the same; the demo must not imply otherwise. | No note. | Disappears on its own once S5 data varies within blocks. |
| D044 | 2026-09-26 | S4 | At 1200 px and wider the map page fills the window height and the controls and panel scroll on their own; below that the page scrolls. The map re-fits the district on every resize until the user pans or zooms. | Keeps the map visible while reading a long panel. Re-fitting fixes a map created while its box was still collapsed. | Page scroll everywhere. | n/a |
